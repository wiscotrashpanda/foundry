---
all:
  children:
    bootstrap:
      hosts:
        foundry_bootstrap:
          ansible_host: foundry
          ansible_connection: ssh
          ansible_user: josh
          ansible_python_interpreter: /usr/bin/python3
    ubuntu:
      hosts:
        foundry:
          ansible_host: foundry
          ansible_connection: ssh
          ansible_user: ansible
          ansible_ssh_private_key_file: ~/.ssh/foundry_ansible
          ansible_python_interpreter: /usr/bin/python3
